<%= form_for [@user, @pretendee, @report], as: :report, html: {class: "  form-group"} do |f| %>
  <div class="row col-sm-4 col-sm-offset-4 input-group">
    <p>You are subscribed for email updates on <%= @pretendee.name %></p>
    <%= f.hidden_field :active, :value => false %>
    <%= f.hidden_field :pretendee_id, :value => @pretendee.id %>
    <span class="input-group-btn"><%= f.submit "Unsubscribe", class: "add-padding-btn btn btn-theme-color btn-lg"%></span>
  </div>
<% end %>
  